---
apiVersion: split.smi-spec.io/v1alpha1
kind: TrafficSplit
metadata:
  name: soroban-pulse-canary
  namespace: default
  labels:
    app: soroban-pulse
    component: service-mesh
spec:
  # Main service that receives traffic
  service: soroban-pulse-service
  
  # Traffic split between stable and canary
  backends:
  - service: soroban-pulse-stable
    weight: 90  # 90% to stable version
  - service: soroban-pulse-canary
    weight: 10  # 10% to canary version

---
apiVersion: v1
kind: Service
metadata:
  name: soroban-pulse-stable
  namespace: default
  labels:
    app: soroban-pulse
    track: stable
spec:
  selector:
    app: soroban-pulse
    track: stable
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  - name: metrics
    port: 9090
    targetPort: 9090

---
apiVersion: v1
kind: Service
metadata:
  name: soroban-pulse-canary
  namespace: default
  labels:
    app: soroban-pulse
    track: canary
spec:
  selector:
    app: soroban-pulse
    track: canary
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  - name: metrics
    port: 9090
    targetPort: 9090

---
# Blue-Green deployment alternative
apiVersion: split.smi-spec.io/v1alpha1
kind: TrafficSplit
metadata:
  name: soroban-pulse-blue-green
  namespace: default
  labels:
    app: soroban-pulse
    component: service-mesh
    deployment-strategy: blue-green
spec:
  service: soroban-pulse-service
  backends:
  - service: soroban-pulse-blue
    weight: 100  # Switch to 0 when promoting green
  - service: soroban-pulse-green
    weight: 0    # Switch to 100 when promoting

---
apiVersion: v1
kind: Service
metadata:
  name: soroban-pulse-blue
  namespace: default
  labels:
    app: soroban-pulse
    deployment: blue
spec:
  selector:
    app: soroban-pulse
    deployment: blue
  ports:
  - name: http
    port: 8080
    targetPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: soroban-pulse-green
  namespace: default
  labels:
    app: soroban-pulse
    deployment: green
spec:
  selector:
    app: soroban-pulse
    deployment: green
  ports:
  - name: http
    port: 8080
    targetPort: 8080
